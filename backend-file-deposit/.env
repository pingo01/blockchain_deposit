# ---------------- 数据库配置（必须和 Navicat 的 mysql-blockchain 连接一致）----------------
DB_HOST=localhost  # 本地 MySQL 固定填 localhost，远程服务器才填 IP
DB_PORT=3306       # MySQL 默认端口，没修改过就是 3306（和 Navicat 连接的端口一致）
DB_USER=root       # 你 Navicat 连接时用的用户名（比如 root，和 mysql-blockchain 连接的用户名一致）
DB_PASSWORD=123456  # 🔴 需要替换：你 Navicat 连接 mysql-blockchain 时用的密码
DB_NAME=blockchain_deposit  # 🔴 需要替换：你在 mysql-blockchain 连接下新建的数据库名（必须一致！）

# ---------------- JWT 配置（和模块二保持一致，不用改）----------------
JWT_SECRET=file_deposit_secret_2025
JWT_EXPIRES_IN=24h  # Token 有效期 24 小时